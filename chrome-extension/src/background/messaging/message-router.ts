/**
 * Stub message router for lightweight background
 */

export const messageRouter = {
  on: () => {},
  emit: () => {},
  broadcastToExtension: () => {},
  destroy: () => {},
  getStats: () => ({ messages: 0, listeners: 0 }),
};
